version: "3"
services:
  client:
    image: "hpilli/multi-client-10-14"
    hostname: client
    mem_limit: 128m
  server:
    image: "hpilli/multi-server-10-14"
    mem_limit: 128m
    hostname: api
    environment:
      - REDIS_HOST=$REDIS_HOST
      - REDIS_PORT=$REDIS_PORT
      - PGUSER=$PGUSER
      - PGHOST=$PGHOST
      - PGDATABASE=$PGDATABASE
      - PGPASSWORD=$PGPASSWORD
      - PGPORT=$PGPORT
  worker:
    image: "hpilli/multi-worker-10-14"
    mem_limit: 128m
    hostname: worker
    environment:
      - REDIS_HOST=$REDIS_HOST
      - REDIS_PORT=$REDIS_PORT
  nginx:
    image: "hpilli/multi-nginx-10-14"
    mem_limit: 128m
    hostname: nginx
    ports:
      - "3000:80"